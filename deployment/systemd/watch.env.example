# FFmpeg Auto-Discovery Watch Daemon Configuration
# Copy this file to /etc/ffrtmp/watch.env and customize

# Scan interval - how often to check for new processes
# Recommended: 10s for production, 5s for dev, 30s for low-priority
SCAN_INTERVAL=10s

# Target commands to discover (comma-separated for CLI, or use config file)
# Examples: ffmpeg, gst-launch-1.0, HandBrakeCLI
TARGET_COMMANDS=ffmpeg

# State persistence flush interval
# How often to save state to disk (default: 30s)
STATE_FLUSH_INTERVAL=30s

# Retry configuration
# Maximum number of retry attempts for failed attachments
MAX_RETRY_ATTEMPTS=5

# Optional: Override configuration file location
# Default: /etc/ffrtmp/watch-config.yaml
# WATCH_CONFIG=/etc/ffrtmp/watch-config.yaml

# Optional: Override state file location
# Default: /var/lib/ffrtmp/watch-state.json
# STATE_PATH=/var/lib/ffrtmp/watch-state.json

# Optional: Log level (debug, info, warn, error)
# Default: info
# LOG_LEVEL=info
