# FFmpeg-RTMP Worker Configuration
# Copy this file to /etc/ffrtmp/worker.env and customize

# Master server URL (required)
MASTER_URL=http://localhost:8080

# API key for authentication (required)
# Get this from master: sudo cat /etc/ffrtmp-master/api-key
API_KEY=your-api-key-here

# Maximum concurrent jobs (default: 4)
MAX_JOBS=4

# Heartbeat interval (default: 30s)
HEARTBEAT_INTERVAL=30s

# Enable auto-attach for running FFmpeg processes (default: false)
# Set to true to enable automatic discovery and attachment
ENABLE_AUTO_ATTACH=false

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Optional: Worker-specific settings
# METRICS_PORT=9091
# POLL_INTERVAL=10s
# INSECURE_SKIP_VERIFY=false
