name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  docker-build-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Create test_results directory
        run: mkdir -p test_results
      
      - name: Build and start services
        run: docker compose up -d --build
      
      - name: Wait for services to be ready
        run: |
          echo "Waiting for services to be ready..."
          for i in {1..30}; do
            if curl -sf http://localhost:9090/-/healthy >/dev/null 2>&1; then
              echo "✓ Services are ready after ${i} seconds"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "✗ Services did not become ready in time"
              exit 1
            fi
            sleep 1
          done
      
      - name: Check Prometheus is up
        run: |
          curl -f http://localhost:9090/-/healthy || exit 1
          echo "✓ Prometheus is healthy"
      
      - name: Check Grafana is up
        run: |
          curl -f http://localhost:3000/api/health || exit 1
          echo "✓ Grafana is healthy"
      
      - name: Check Alertmanager is up
        run: |
          curl -f http://localhost:9093/-/healthy || exit 1
          echo "✓ Alertmanager is healthy"
      
      - name: Check exporters are responding
        run: |
          # RAPL exporter requires privileged access to /sys/class/powercap
          # which is not available in GitHub Actions, so we don't fail on it
          curl -f http://localhost:9500/metrics || echo "⚠ RAPL exporter requires privileged access (expected in CI)"
          
          # These exporters should work in CI environment
          curl -f http://localhost:9501/metrics || exit 1
          echo "✓ Docker stats exporter is responding"
          curl -f http://localhost:9502/health || exit 1
          echo "✓ Results exporter is healthy"
          curl -f http://localhost:9503/health || exit 1
          echo "✓ QoE exporter is healthy"
          curl -f http://localhost:9504/health || exit 1
          echo "✓ Cost exporter is healthy"
      
      - name: Show running containers
        if: always()
        run: docker compose ps
      
      - name: Show logs on failure
        if: failure()
        run: docker compose logs --tail=100
      
      - name: Tear down services
        if: always()
        run: docker compose down -v
